# This app needs no build-time secrets.
#
# The OpenRouter API key is entered in the app itself (AI Engine settings) and
# is stored in the browser's local storage, so the published site never carries
# a key and every visitor uses their own.
#
# Get a key at https://openrouter.ai/keys
